<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Redirigir solicitudes a index.php si no son archivos o directorios reales
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L]
</IfModule>
